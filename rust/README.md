# logtail

A live log tailer with built-in filtering — like `tail -f` and `grep` in
one small CLI. Point it at a file, watch new lines stream in, and keep
only the ones you care about.

> **Coming soon.** The app itself is being built up PR by PR. Run and
> architecture notes land with the scaffold. For now, see
> [`ROADMAP.md`](ROADMAP.md) for what's planned and what's open to build.

## Run

_(Added when the app is scaffolded.)_

## Architecture

_(Added when the app is scaffolded.)_
